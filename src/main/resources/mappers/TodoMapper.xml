<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.samdul.hellojava.mapper.TodoMapper"> <!-- 패키지 경로 수정 -->
    <select id="findAll" resultType="shop.samdul.hellojava.entity.TodoEntity"> <!-- 패키지 경로 수정 -->
        SELECT
            id, subject, body, completed
        FROM ENCORE_TODO_TBL
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="shop.samdul.hellojava.entity.TodoEntity"> <!-- 패키지 경로 수정 -->
        SELECT
            id, subject, body, completed
        FROM ENCORE_TODO_TBL
        WHERE id = #{id}
    </select>

    <insert id="add" parameterType="map">
        INSERT INTO ENCORE_TODO_TBL(subject, body, completed)
        VALUES(#{subject}, #{body}, #{completed})
    </insert>

    <update id="updateByPut" parameterType="map">
        UPDATE ENCORE_TODO_TBL
        SET SUBJECT = #{subject}, BODY = #{body}, completed = #{completed}
        WHERE id = #{id}
    </update>

    <update id="updateByPatch" parameterType="map">
        UPDATE ENCORE_TODO_TBL
        SET SUBJECT = #{subject}, BODY = #{body}, completed = #{completed}
        WHERE id = #{id}
    </update>
    
    <delete id="delete" parameterType="int">
        DELETE FROM ENCORE_TODO_TBL
        WHERE id = #{id}
    </delete>
</mapper>
